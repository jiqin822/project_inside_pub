# Copy to .env.development (or rely on defaults).
# Local dev uses local backend/db. Production build uses .env.production for remote API.
# Invite links use same host, port 3000 (consistent with API URL).
VITE_API_BASE_URL=http://localhost:8000
