# Production API URL (build-time). Set this in DigitalOcean App Platform
# on the web component as an env var, or copy to .env.production for local production builds.
# Use your app URL — same domain for frontend and /v1 API (no trailing slash).
# Invite links use this same origin (consistent with API URL).
VITE_API_BASE_URL=https://project-inside-c6bdb.ondigitalocean.app/project-inside-backend
