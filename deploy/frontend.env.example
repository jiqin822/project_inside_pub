# Frontend build env for remote deploy. Used as VITE_API_BASE_URL when building the frontend image.
# Same origin as your deployed app (no trailing slash). Nginx proxies /v1 to backend.

# Production API URL (build-time)
VITE_API_BASE_URL=https://your-domain.com
