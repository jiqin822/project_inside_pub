# Database
# Note: Docker-compose maps PostgreSQL to port 5433
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5433/project_inside

# Redis
REDIS_URL=redis://localhost:6379/0
REDIS_QUEUE_URL=redis://localhost:6379/1

# Security
SECRET_KEY=change-me-in-production-use-env-var
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=30

# CORS (origins that may send requests to the API)
# Local:
CORS_ORIGINS=["http://localhost:*", "http://127.0.0.1:*", "http://192.168.*.*:*"]
# Cloud (DigitalOcean): set on api component to your app URL
# CORS_ORIGINS=["https://project-inside-c6bdb.ondigitalocean.app"]

# Email & App
APP_PUBLIC_URL=http://localhost:3000
EMAIL_BLOCKED_DOMAINS=

# SendGrid Email Service (optional - if not set, uses console email service)
# Get your API key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=
EMAIL_FROM_ADDRESS=noreply@seerbyse.ai
EMAIL_FROM_NAME=Project Inside

# Gemini API (for activity recommendations and Love Map)
# Get your key from: https://aistudio.google.com/apikey
# If unset or empty, activity cards use seed/fallback data instead of LLM.
GEMINI_API_KEY=

# OpenAI API (scrapbook sticker image generation via GPT Image / gpt-image-1)
# Get your key from: https://platform.openai.com/api-keys
# If unset or empty, scrapbook stickers are skipped or fall back to Gemini.
OPENAI_API_KEY=

# Push notifications (FCM)
# Set to true when Firebase is configured; requires GOOGLE_APPLICATION_CREDENTIALS (path to service account JSON)
PUSH_ENABLED=false
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/firebase-service-account.json

# GCP Speech-to-Text v2
# Credentials: use one of:
#   - GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json (local)
#   - GOOGLE_APPLICATION_CREDENTIALS_JSON=<entire JSON string> (DigitalOcean / PaaS; no file path)
# IAM: the service account must have role "Cloud Speech Client" (roles/speech.client) so it has
#   speech.recognizers.recognize. In Console: IAM & Admin → find the SA → Edit → Add role → Cloud Speech-to-Text Client.
# Full resource name: projects/PROJECT_ID/locations/LOCATION/recognizers/NAME (e.g. livecoach from gcloud alpha ml speech recognizers list --location=us)
STT_RECOGNIZER=projects/YOUR_PROJECT/locations/us/recognizers/livecoach
STT_SPEAKER_MATCH_THRESHOLD=0.3
STT_SPEAKER_MATCH_MARGIN=0.08
STT_AUDIO_BUFFER_SECONDS=30
STT_ESCALATION_COOLDOWN_SECONDS=5

# Optional NeMo speaker diarization fallback (when Google diarization unavailable, e.g. language_code=auto).
# See backend/docs/NEMO_SETUP.md for full setup.
# Set to false if NeMo is not installed to avoid startup warning.
# STT_ENABLE_NEMO_DIARIZATION_FALLBACK=false
# Optional: path to local .nemo checkpoint (else downloads nvidia/diar_streaming_sortformer_4spk-v2.1 from HF)
# NEMO_DIAR_MODEL_PATH=/path/to/diar_streaming_sortformer_4spk-v2.1.nemo
# Optional: Hugging Face token (for gated models); NEMO_CACHE_DIR for cache directory
# HF_TOKEN=
# NEMO_CACHE_DIR=
GOOGLE_APPLICATION_CREDENTIALS_JSON=

GEMINI_API_KEY=

OPENAI_API_KEY=

STT_RECOGNIZER=projects/YOUR_PROJECT/locations/us/recognizers/livecoach

